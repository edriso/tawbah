{
  "name": "tawbah",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "توبة — مساحة هادئة للاستغفار، مع جلسات مؤقتة وتذكير صوتي اختياري.",
  "scripts": {
    "dev": "node scripts/quran.mjs && vite --host 127.0.0.1",
    "build": "node scripts/quran.mjs && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "prettier": "^3.9.6",
    "vite": "^7.3.1"
  }
}
